---
short_sha: 6c573b4
status: not-applicable
files_changed: []
---
The audit target `src/server/database/records/longout.rs::init_record` does not exist (the actual path is `src/server/records/longout.rs`), and the `OOPT`/`On Change` mechanism is not implemented at all.

`LongoutRecord` (`src/server/records/longout.rs`) does not declare `oopt`, `outpvt`, `ooch`, or `pval` fields, has no `conditional_write()` helper, and provides no `init_record` override that could mis-initialize an `OUT_LINK_UNCHANGED` state. Fields actually present cover VAL/EGU/HOPR/LOPR/DRVH/DRVL/IVOA/IVOV/ADEL/MDEL/OMSL/DOL/SIMM/SIML/SIOL/SIMS plus alarm thresholds (lines 5-65); the OOPT-related state machine the C fix targeted is wholly absent.

Because the `OOPT = On Change` skip-on-first-process logic does not exist in the Rust impl, there is no `outpvt` initialization to correct. When OOPT is added, the audit recommendation (initialize the equivalent of `outpvt` to "execute" / `EXEC_OUTPUT` and gate `special()`'s flag on `after && ooch == YES`) should be revisited.

The bug pattern is structurally absent.
