---
short_sha: 5d1f572
status: not-applicable
files_changed: []
---
The audit targets `src/server/database/dev/aai_soft.rs::read_aai` and `dev/wf_soft.rs::read_wf` do not exist. The entire `src/server/database/dev/` directory is absent from `epics-base-rs`.

There is no `aai` record in the Rust tree (verified by grep: no `aai`, `aaiRecord`, or `AaiRecord`). The `WaveformRecord` (`src/server/records/waveform.rs`) is a single record impl with `put_field` updating `nord` directly from `VAL` writes (`waveform.rs:218-261`); it has no separate "soft device support" layer that could double-post NORD.

Architecturally, monitor notification in `epics-base-rs` flows only through `RecordInstance::notify_from_snapshot` (called from `processing.rs` after `record.process()`). There is no device-support trait API that exposes `db_post_events` to lower layers, so the duplicate-posting bug class cannot occur.

The bug pattern is structurally absent.
