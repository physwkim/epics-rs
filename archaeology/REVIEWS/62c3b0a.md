---
short_sha: 62c3b0a
status: not-applicable
files_changed: []
---
The audit target `src/log/ioc_log.rs::ioc_log_client_init` does not exist. The entire `src/log/` directory is absent from `epics-base-rs`. A grep across the crate finds zero references to `ioc_log`, `errlog`, `LogClient`, `errlogAddListener`, or any related symbol.

`epics-base-rs` does not yet implement the IOC log client / errlog subscriber bridge. There is no generic `LogClient` constructor that could subscribe itself, and no IOC-specific wrapper to mis-route subscriptions through. The fan-out / duplicate-listener routing bug from `logClient.c` cannot exist because the affected code path is not present.

When the log infrastructure is implemented later, the audit recommendation (errlog subscription belongs in the IOC-level wrapper, not the generic `LogClient` constructor; ensure `Drop` unregisters) should be revisited.
