---
short_sha: 3fb10b6
status: not-applicable
files_changed: []
---
The audit target `src/server/database/db_notify.rs::process_notify_common` does not exist. `epics-base-rs` does not implement the `dbProcessNotify` chain-with-restart pattern; there is no `processNotifyCommon`/`notifyCallback` split that could re-set PUTF on a restart.

The `PUTF` flag (`record::common_fields::CommonFields::putf`) is set in only three places, each of which corresponds exactly to the C "first call" semantic the bug fix introduces:
- `src/server/database/field_io.rs:363` — initial CA/PVA put (the moral equivalent of `dbProcessNotify` with `first=1`).
- `src/server/database/processing.rs:844` and `:1344` — CP-link target dispatch, which already gates on "not already processing" (sets `rpro` instead when active), matching the C parity comment referencing commit `a4bc0db`.

There is no async restart code path that re-enters and re-sets `putf`. The `put_notify_tx` oneshot completion (`record_instance.rs:87`) only fires the callback; it never re-flags `putf`. The bug pattern is structurally absent.
