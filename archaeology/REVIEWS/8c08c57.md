---
short_sha: 8c08c57
status: not-applicable
files_changed: []
---
The audit target `src/log/err_sym.rs::err_symbol_add` does not exist. The entire `src/log/` directory is absent from `epics-base-rs`. A grep finds zero references to `err_sym`, `errSym`, `errSymbol`, `errSymbolAdd`, or `errSymBld`.

`epics-base-rs` does not yet implement the error-symbol registry. There is no `ELLLIST errnumlist`-equivalent linked list, no `initialized` flag, no two-phase `errSymBld`/`errSymbolAdd` split that could allow add-before-build symbol losses. Because the affected subsystem is entirely unbuilt, the C ordering / thread-safety bug pattern cannot occur.

When this registry is added later, the audit recommendation (lazy init via `OnceLock`/`Lazy`, mutex-protected insertion, duplicate-code error semantics, module number ≥ 501 validation) should be revisited.
