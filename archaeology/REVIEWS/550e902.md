---
short_sha: 550e902
title: iocLogPrefix warns on identical re-set instead of accepting silently
status: not-applicable
crate: base-rs
---

# Review

## Audit Targets
- `src/log/log_client.rs::ioc_log_prefix` — the entire `src/log/`
  directory does not exist in `epics-base-rs` (no `log_client.rs`,
  no `ioc_log_prefix` symbol anywhere in the crate).

## Verification
- `ls crates/epics-base-rs/src/` shows only:
  `calc/ error.rs lib.rs net/ runtime/ server/ types/`.
- Grep for `ioc_log_prefix`, `iocLogPrefix`, `log_prefix` in
  `epics-base-rs/src/`: no matches.

base-rs has no IOC log-client subsystem, so there is no idempotent
prefix-set guard to add.

## Decision
**not-applicable** — the entire feature surface (log client + prefix
configuration) is absent from base-rs.

## C Reference
- `modules/libcom/src/log/logClient.c:iocLogPrefix` — missing strcmp before warning
</content>
</invoke>