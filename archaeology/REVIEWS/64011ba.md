---
short_sha: 64011ba
status: not-applicable
files_changed: []
---
The audit target `src/server/database/dev/sa_soft.rs::read_sa` does not exist. The `src/server/database/dev/` directory is absent, and there is no `subArray` record in `epics-base-rs` (no `sa_soft.rs`, no `SubArrayRecord`, no references to `subArray`).

Because there is no soft-device-support layer for `subArray` (and no `subArray` record at all), the duplicated `db_post_events(&prec->nord, ...)` call site does not exist. More broadly, `epics-base-rs` does not expose a notification API to any device-support layer: monitor events flow only through `RecordInstance::notify_from_snapshot` (`src/server/record/record_instance.rs:1397`) invoked by `processing.rs` after `record.process()`. The architectural invariant the C fix restored is already structurally enforced by the Rust trait boundaries.

The bug pattern is structurally absent.
