---
short_sha: 6e7a715
status: not-applicable
files_changed: []
---
The C bug was `dbFastLinkConv.c::cvt_device_st` collapsing four guard conditions (`!paddr || !pdbFldDes || !pdbDeviceMenu || *from >= nChoice`) into one compound `if` that returned `S_db_badChoice` for all of them — including the legitimate "record type has no device support" case where the correct semantics is to return an empty string. In `epics-base-rs` there is no `dbFastLinkConv` / `cvt_device_st` equivalent: `DTYP` is a plain `String` field on `CommonFields` (`server/record/common_fields.rs:31`), read via `RecordInstance::get_field` (`record_instance.rs:560` → `Some(EpicsValue::String(self.common.dtyp.clone()))`) and written at line 750. There is no `dbDeviceMenu`, no `papChoice` table indexed by `*from`, no `ftPvt`/`devSup-list` lookup, and no Rust-side menu→string conversion path that can fail. Reading `.DTYP` from a record with no device support simply returns whatever string was configured at iocBuilder time (often empty), which is exactly the post-fix C semantics. Structurally absent.
