---
short_sha: f6e8a75
status: not-applicable
files_changed: []
---
The audit target `src/server/database/db_link.rs::db_db_get_value` does not exist in `epics-base-rs`. The crate has no `db_link.rs`, no `dbChannelFinalFieldType`/`dbChannelFieldType` distinction, and no scalar fast-path that dispatches on a "final" vs "original" channel field type. DB link reads in base-rs go through `src/server/database/links.rs::read_link_value`, which is a name-based lookup that calls `get_pv("RECORD.FIELD")` and returns an `EpicsValue` directly — there is no `dbChannelOpen`-style probe that promotes `DBF_MENU` to `DBF_ENUM`. Furthermore, `DbFieldType` in `src/types/dbr.rs` has only seven variants (String/Short/Float/Enum/Char/Long/Double) and explicitly maps `DBF_MENU`+`DBF_DEVICE` onto `DbFieldType::Enum` from the start (see comment at `src/types/dbr.rs:73-78`), so the type-promotion confusion the C fix addresses cannot occur. Nothing to fix.
