---
short_sha: e860617
status: not-applicable
files_changed: []
---
The audit target `src/server/database/dbDbLink.rs::process_target` does not exist in `epics-base-rs`. The Rust rewrite has no `process_target` function and no per-record `procThread` ownership marker — link-driven processing flows through `process_record_with_links` in `src/server/database/processing.rs`, gated by a `visited: HashSet<String>` cycle guard plus an `AtomicBool` `processing` flag on each `RecordInstance`. The C bug requires a same-thread recursive entry into a DB-linked target where `psrc->putf=1` and `pdst->pact=1`; in base-rs the equivalent CP-link dispatch (processing.rs:825-849) explicitly skips already-visited targets and only sets `rpro=true` when the target's `processing` flag is set without re-entering — so the recursive double-RPRO scenario the C fix addresses is structurally absent. No per-record "current processor task ID" marker is needed because `visited` tracks the recursion chain directly.
