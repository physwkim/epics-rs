---
short_sha: 51c5b8f
status: not-applicable
files_changed: []
---
The audit target `src/server/database/rec/subarray.rs::process` does not exist. `epics-base-rs` has no `subArray` record at all (verified by grep: no `sub_array`, `SubArrayRecord`, or `subarray.rs` anywhere under `crates/epics-base-rs/src/`). The `rec/` directory itself does not exist; records live under `src/server/records/`.

Because the record is not implemented, there is no `process()` to forget to post NORD events from. When a `subArray` record is added, the architecture already enforces the correct layering: monitor notification flows exclusively through `RecordInstance::notify_from_snapshot` (`src/server/record/record_instance.rs:1397`) called from `processing.rs` after `record.process()` returns — there is no device-support trait method that could call into the notify API.

The bug pattern is structurally absent.
