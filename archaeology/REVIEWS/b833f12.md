---
short_sha: b833f12
status: already-fixed
files_changed: []
---
The targeted file `src/util/stdlib.rs` does not exist; base-rs has no hand-rolled `epicsStrtod` wrapper. The closest analog is `EpicsValue::parse_int` in `crates/epics-base-rs/src/types/value.rs:656` which already uses `i64::from_str_radix` for hex/octal — that is a 64-bit parse on every Rust target (including 32-bit), so the C `strtol`/`strtoul` width bug cannot occur. The `Double` parse path uses `f64::parse`, which doesn't accept hex but also can't underflow to 32 bits. No change needed.
