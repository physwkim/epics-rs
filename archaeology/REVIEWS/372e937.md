---
short_sha: 372e937
title: dbGet — duplicated dbfl_type_val/ref dispatch replaced with dbfl_pfield macro
status: not-applicable
crate: base-rs
---

# Review

## Audit Targets
- `src/server/database/db_access.rs::db_get` — file exists
  (`crates/epics-base-rs/src/server/database/db_access.rs`, 413 lines)
  but contains no `DbFieldLog` / field-log union dispatch.

## Verification
Searched the entire `epics-base-rs` source tree for `DbFieldLog`,
`db_field_log`, `dbfl_type`, `FieldLog`, `field_log` — no matches.

base-rs is a high-level rewrite that does not implement the
`db_field_log` shadow-buffer mechanism used by C `dbGet` for monitor
post-processing. There is no val/ref union to abstract.

## Decision
**not-applicable** — the data structure the C macro wraps does not
exist in base-rs. The maintainability concern flagged by the C commit
has no equivalent here.

## C Reference
- `modules/database/src/ioc/db/db_field_log.h:dbfl_pfield` — new macro
- `modules/database/src/ioc/db/dbAccess.c:dbGet` — two call sites replaced
</content>
</invoke>