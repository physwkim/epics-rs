# R126 — 785e180f9bce [LOW][partial]

**Subject**: evhelper: ensure work dtor before notify  
**Date**: 2020-12-29  
**pvxs SHA**: 785e180f9bce  

## pva-rs mapping
- (no direct mapping found)

## Verdict
**partial** — LOW

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit 785e180f9bcec877af215761290992116cd31b47
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Tue Dec 29 12:49:05 2020 -0800

    evhelper: ensure work dtor before notify
    
    Ensure that work std::function, and any bound variables,
    are destroyed before completion notification.
---
 src/evhelper.cpp | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/src/evhelper.cpp b/src/evhelper.cpp
index fb7c30c..dea6b01 100644
--- a/src/evhelper.cpp
+++ b/src/evhelper.cpp
@@ -201,9 +201,10 @@ struct evbase::Pvt : public epicsThreadRunable
             todo = std::move(actions);
         }
         for(auto& work : todo) {
             try {
-                work.fn();
+                auto fn(std::move(work.fn));
+                fn();
             }catch(std::exception& e){
                 if(work.result) {
                     Guard G(lock);
                     *work.result = std::current_exception();

```
