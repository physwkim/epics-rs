# R127 — 8ed998a89605 [LOW][partial]

**Subject**: sharedpv: fix race w/ current  
**Date**: 2021-09-21  
**pvxs SHA**: 8ed998a89605  

## pva-rs mapping
- crates/epics-pva-rs/src/server_native/shared_pv.rs

## Verdict
**partial** — LOW

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit 8ed998a896051801b8b239ad868abebf37288cce
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Tue Sep 21 10:24:16 2021 -0700

    sharedpv: fix race w/ current
    
    Don't allow current Value to become queued,
    where it would be accessed w/o SharedPV::lock
---
 src/sharedpv.cpp | 6 ++++--
 1 file changed, 4 insertions(+), 2 deletions(-)

diff --git a/src/sharedpv.cpp b/src/sharedpv.cpp
index 37804ed..09ee256 100644
--- a/src/sharedpv.cpp
+++ b/src/sharedpv.cpp
@@ -261,9 +261,9 @@ void SharedPV::attach(std::unique_ptr<ChannelControl>&& ctrlop)
 
                 self->mpending.insert(std::move(conn));
 
             } else {
-                temp = self->current;
+                temp = self->current.clone();
             }
         }
         if(temp)
             Impl::connectSub(self, conn, temp);
@@ -358,9 +358,11 @@ void SharedPV::open(const Value& initial)
 
         pending = std::move(impl->pending);
         mpending = std::move(impl->mpending);
 
-        impl->current = temp = initial.clone();
+        impl->current = initial.clone();
+        // make a second copy as 'temp' will be queued
+        temp = initial.clone();
     }
 
     // TODO the following is really inefficient if we aren't on a worker.
     //      API to batch?

```
