# R033 — 8d58409481ef [MEDIUM][applies]

**Subject**: server: check tx buffer limit to throttle  
**Date**: 2023-10-16  
**pvxs SHA**: 8d58409481ef  

## pva-rs mapping
- crates/epics-pva-rs/src/server_native/tcp.rs (MONITOR handler)

## Verdict
**applies** — MEDIUM

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit 8d58409481efd3f6d648abe168ff483775f32359
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Mon Oct 16 18:32:26 2023 -0700

    server: check tx buffer limit to throttle
    
    The TX buffer could grow while nothing is being received.
    Practically bounded by the timeout interval, but could
    still get quite large in that time.
---
 src/servermon.cpp | 4 +++-
 1 file changed, 3 insertions(+), 1 deletion(-)

diff --git a/src/servermon.cpp b/src/servermon.cpp
index dae40c7..27c4244 100644
--- a/src/servermon.cpp
+++ b/src/servermon.cpp
@@ -89,9 +89,11 @@ struct MonitorOp final : public ServerOp
                 auto conn(ch->conn.lock());
                 if(!conn || conn->state==ConnBase::Disconnected)
                     return;
 
-                if(conn->connection() && (bufferevent_get_enabled(conn->connection())&EV_READ)) {
+                auto bev(conn->connection());
+
+                if(bev && evbuffer_get_length(bufferevent_get_output(bev)) < conn->tcp_tx_limit) {
                     doReply(op);
                 } else {
                     // connection TX queue is too full
                     conn->backlog.emplace_back([op]() { doReply(op); });

```
