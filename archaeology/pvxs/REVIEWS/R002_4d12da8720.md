# R002 — 4d12da87205e [HIGH][applies]

**Subject**: client: don't attempt to reconnect NS during shutdown  
**Date**: 2023-05-11  
**pvxs SHA**: 4d12da87205e  

## pva-rs mapping
- crates/epics-pva-rs/src/client_native/tcp.rs

## Verdict
**applies** — HIGH

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit 4d12da87205e9ae80737027dbc7151d8ed9697ec
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Thu May 11 11:23:00 2023 -0700

    client: don't attempt to reconnect NS during shutdown
    
    Connection::build() can't succeed
---
 src/client.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/client.cpp b/src/client.cpp
index f74b29e..cdc3ae0 100644
--- a/src/client.cpp
+++ b/src/client.cpp
@@ -206,9 +206,9 @@ void Channel::disconnect(const std::shared_ptr<Channel>& self)
         log_debug_printf(io, "Server %s detach channel '%s' to re-search\n",
                          current ? current->peerName.c_str() : "<disconnected>",
                          name.c_str());
 
-    } else { // reconnect to specific server
+    } else if(context->state==ContextImpl::Running) { // reconnect to specific server
         conn = Connection::build(context, forcedServer, true);
 
         conn->pending[cid] = self;
         state = Connecting;

```
