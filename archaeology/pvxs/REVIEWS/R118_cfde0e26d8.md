# R118 — cfde0e26d85a [LOW][applies]

**Subject**: avoid assert for mostly absurdly long PV names  
**Date**: 2022-10-13  
**pvxs SHA**: cfde0e26d85a  

## pva-rs mapping
- crates/epics-pva-rs/src/client_native/tcp.rs

## Verdict
**applies** — LOW

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit cfde0e26d85a070a670c20d6c555e3887671ff58
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Thu Oct 13 16:44:15 2022 -0700

    avoid assert for mostly absurdly long PV names
    
    Longer than typical ethernet MTU,
    but less than UDP packet limit.
---
 src/client.cpp | 15 +++++++++++----
 1 file changed, 11 insertions(+), 4 deletions(-)

diff --git a/src/client.cpp b/src/client.cpp
index 189cab8..c6d4771 100644
--- a/src/client.cpp
+++ b/src/client.cpp
@@ -1011,12 +1011,19 @@ void ContextImpl::tickSearch(bool discover)
                 bucket.pop_front();
                 continue;
 
             } else if(size_t(M.save() - searchMsg.data()) > maxSearchPayload) {
-                assert(payload); // must have something
-                // too large, defer
-                M.restore(save);
-                break;
+                if(payload) {
+                    // other names did fit, defer this one to the next packet
+                    M.restore(save);
+                    break;
+
+                } else {
+                    // some slightly less absurdly long PV name.
+                    // Less than the UDP packet limit, but longer
+                    // than typical MTU.  Try to send, probably
+                    // no choice but to fragment.
+                }
             }
 
             count++;
 

```
