# R042 — b38b33db034e [MEDIUM][applies]

**Subject**: raise search reply processing limit  
**Date**: 2021-01-26  
**pvxs SHA**: b38b33db034e  

## pva-rs mapping
- crates/epics-pva-rs/src/client_native/tcp.rs

## Verdict
**applies** — MEDIUM

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit b38b33db034e67a8d6946f16527480529a7c830d
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Tue Jan 26 21:50:15 2021 -0800

    raise search reply processing limit
    
    Seems pvAccessCPP likes to send lots of individual replies
    to our nicely batched search requests.
---
 src/client.cpp | 7 +++++--
 1 file changed, 5 insertions(+), 2 deletions(-)

diff --git a/src/client.cpp b/src/client.cpp
index a4ac99b..7ea956e 100644
--- a/src/client.cpp
+++ b/src/client.cpp
@@ -613,10 +613,13 @@ void Context::Pvt::onSearchS(evutil_socket_t fd, short evt, void *raw)
         log_debug_printf(io, "UDP search Rx event %x\n", evt);
         if(!(evt&EV_READ))
             return;
 
-        // handle up to 4 packets before going back to the reactor
-        for(unsigned i=0; i<4 && static_cast<Pvt*>(raw)->onSearch(); i++) {}
+        // limit number of packets processed before going back to the reactor
+        unsigned i;
+        const unsigned limit = 40;
+        for(i=0; i<limit && static_cast<Pvt*>(raw)->onSearch(); i++) {}
+        log_debug_printf(io, "UDP search processed %u/%u\n", i, limit);
 
     }catch(std::exception& e){
         log_exc_printf(io, "Unhandled error in search Rx callback: %s\n", e.what());
     }

```
