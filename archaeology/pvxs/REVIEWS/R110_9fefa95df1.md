# R110 — 9fefa95df1f7 [LOW][applies]

**Subject**: fix client PUT  
**Date**: 2020-02-25  
**pvxs SHA**: 9fefa95df1f7  

## pva-rs mapping
- crates/epics-pva-rs/src/client_native/{get,put,rpc}.rs

## Verdict
**applies** — LOW

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit 9fefa95df1f740e4fce4d5793e743d871408be99
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Tue Feb 25 15:15:04 2020 -0800

    fix client PUT
---
 src/clientget.cpp | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/src/clientget.cpp b/src/clientget.cpp
index f8e71d0..ecced26 100644
--- a/src/clientget.cpp
+++ b/src/clientget.cpp
@@ -301,13 +301,14 @@ void Connection::handle_GPR(pva_app_msg_t cmd)
 
         to_wire(R, op->chan->sid);
         to_wire(R, ioid);
         if(gpr->state==GPROp::GetOPut) {
-            to_wire(R, 0x40);
+            to_wire(R, uint8_t(0x40));
 
         } else if(gpr->state==GPROp::Exec) {
-            to_wire(R, 0x00);
-            to_wire_valid(R, info->prototype);
+            to_wire(R, uint8_t(0x00));
+            if(cmd!=CMD_GET)
+                to_wire_valid(R, info->prototype);
 
         } else if(gpr->state==GPROp::Done) {
             // we're actually building CMD_DESTROY_REQUEST
             // nothing more needed

```
