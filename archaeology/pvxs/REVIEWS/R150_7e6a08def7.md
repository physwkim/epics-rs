# R150 — 7e6a08def72e [LOW][uncertain]

**Subject**: fix Delta print of Union  
**Date**: 2020-04-17  
**pvxs SHA**: 7e6a08def72e  

## pva-rs mapping
- (no direct mapping found)

## Verdict
**uncertain** — LOW

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit 7e6a08def72e6953605f2320b90da4e78339a59a
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Fri Apr 17 14:15:16 2020 -0700

    fix Delta print of Union
---
 src/datafmt.cpp   | 3 ++-
 test/testtype.cpp | 1 +
 2 files changed, 3 insertions(+), 1 deletion(-)

diff --git a/src/datafmt.cpp b/src/datafmt.cpp
index 8844d81..88de1c9 100644
--- a/src/datafmt.cpp
+++ b/src/datafmt.cpp
@@ -15,9 +15,9 @@ struct FmtDelta {
     const Value::Fmt& fmt;
 
     void field(const std::string& prefix, const Value& val, bool verytop)
     {
-        if(!val.isMarked(true,false))
+        if(verytop && !val.isMarked())
             return;
 
         strm<<prefix;
         if(!verytop)
@@ -62,8 +62,9 @@ struct FmtDelta {
                 auto udesc = Value::Helper::desc(uval);
                 for(auto idx : range(desc->members.size())) {
                     if(udesc == &desc->members[idx]) {
                         cprefix+=desc->miter[idx].first;
+                        break;
                     }
                 }
             }
 
diff --git a/test/testtype.cpp b/test/testtype.cpp
index 31797cf..d2def22 100644
--- a/test/testtype.cpp
+++ b/test/testtype.cpp
@@ -415,8 +415,9 @@ void testFormat()
         "array.choice[0] union\n"
         "array.choice[0]->one int32_t = 1357\n"
         "array.choice[1] null\n"
         "array.choice[2] union\n"
+        "array.choice[2]->two struct\n"
         "array.choice[2]->two.ahalf int32_t = 2468\n"
         "array.more struct[] = {\?}[]\n"
     );
 }

```
