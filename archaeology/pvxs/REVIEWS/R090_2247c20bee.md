# R090 — 2247c20bee44 [LOW][applies]

**Subject**: quiet MONITOR exceeds window size  
**Date**: 2023-05-15  
**pvxs SHA**: 2247c20bee44  

## pva-rs mapping
- crates/epics-pva-rs/src/client_native/monitor.rs

## Verdict
**applies** — LOW

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit 2247c20bee44e2cc1acca8c112c04bfe07027c6a
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Mon May 15 10:31:30 2023 -0700

    quiet MONITOR exceeds window size
    
    The destroy message triggers a spurious ERR.
---
 src/clientmon.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/clientmon.cpp b/src/clientmon.cpp
index dd0f782..4850e6a 100644
--- a/src/clientmon.cpp
+++ b/src/clientmon.cpp
@@ -709,9 +709,9 @@ void Connection::handle_MONITOR()
                     if(!mon->window)
                         log_debug_printf(io, "Server %s channel '%s' MONITOR zero window w/ %u\n",
                                         peerName.c_str(), mon->chan->name.c_str(), unsigned(mon->unack));
 
-                } else {
+                } else if(!final) {
                     log_err_printf(io, "Server %s channel '%s' MONITOR exceeds window size\n",
                                     peerName.c_str(), mon->chan->name.c_str());
                 }
             }

```
