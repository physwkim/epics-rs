# R120 — dd2f076b4aa6 [LOW][applies]

**Subject**: client: only advance search bucket during normal search  
**Date**: 2023-04-17  
**pvxs SHA**: dd2f076b4aa6  

## pva-rs mapping
- crates/epics-pva-rs/src/client_native/tcp.rs

## Verdict
**applies** — LOW

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit dd2f076b4aa6dc63fb91980eda711eabee910696
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Mon Apr 17 15:24:26 2023 -0500

    client: only advance search bucket during normal search
---
 src/client.cpp | 4 +++-
 1 file changed, 3 insertions(+), 1 deletion(-)

diff --git a/src/client.cpp b/src/client.cpp
index c72c186..c239c76 100644
--- a/src/client.cpp
+++ b/src/client.cpp
@@ -1072,9 +1072,11 @@ void ContextImpl::tickSearch(SearchKind kind)
             }
 
             count++;
 
-            auto ninc = chan->nSearch = std::min(searchBuckets.size(), chan->nSearch+1u);
+            size_t ninc = 0u;
+            if(kind==SearchKind::check)
+                ninc = chan->nSearch = std::min(searchBuckets.size(), chan->nSearch+1u);
             auto next = (idx + ninc)%searchBuckets.size();
             auto nextnext = (next + 1u)%searchBuckets.size();
 
             // try to smooth out UDP bcast load by waiting one extra tick

```
