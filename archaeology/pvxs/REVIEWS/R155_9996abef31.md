# R155 — 9996abef3159 [LOW][uncertain]

**Subject**: fix Value::isMarked parents=true  
**Date**: 2020-03-19  
**pvxs SHA**: 9996abef3159  

## pva-rs mapping
- crates/epics-pva-rs/src/protocol/decode.rs + encode.rs

## Verdict
**uncertain** — LOW

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit 9996abef3159d682a7f18da9ec4022c3a089255e
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Thu Mar 19 14:22:18 2020 -0700

    fix Value::isMarked parents=true
---
 src/data.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/data.cpp b/src/data.cpp
index dec710c..d8d7a7d 100644
--- a/src/data.cpp
+++ b/src/data.cpp
@@ -330,10 +330,10 @@ bool Value::isMarked(bool parents, bool children) const
     if(parents) {
         auto pdesc = desc;
         auto pstore = store.get();
         while(pdesc!=top->desc.get()) {
-            pdesc -= pdesc->parent_index;
             pstore -= pdesc->parent_index;
+            pdesc -= pdesc->parent_index;
 
             if(pstore->valid)
                 return true;
         }

```
