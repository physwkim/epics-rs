# R131 — 056fb2c27b40 [LOW][uncertain]

**Subject**: fix iteration of sub-struct  
**Date**: 2020-03-07  
**pvxs SHA**: 056fb2c27b40  

## pva-rs mapping
- crates/epics-pva-rs/src/protocol/decode.rs + encode.rs

## Verdict
**uncertain** — LOW

> TODO: manual review — does this bug exist in pva-rs?

## Diff
```diff
commit 056fb2c27b40a91e0ac68d05833e191ff01bfed3
Author: Michael Davidsaver <mdavidsaver@gmail.com>
Date:   Sat Mar 7 17:31:00 2020 -0800

    fix iteration of sub-struct
---
 src/data.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/data.cpp b/src/data.cpp
index 975aa63..a7cbeb0 100644
--- a/src/data.cpp
+++ b/src/data.cpp
@@ -745,9 +745,9 @@ void Value::_iter_fl(Value::IterInfo &info, bool first) const
     if(!store)
         throw NoField();
 
     if(info.depth) {
-        info.pos = info.nextcheck = store->index() + (first ? 1u : desc->size());
+        info.pos = info.nextcheck = first ? 1u : desc->size();
 
         if(info.marked)
             _iter_advance(info);
 

```
