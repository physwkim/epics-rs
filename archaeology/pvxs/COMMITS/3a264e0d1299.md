# 3a264e0d1299 — Fix missing pointer dereference

**Date**: 2020-06-29  
**Author**: karlosp  
**Severity**: MEDIUM  
**Verdict**: uncertain  

## Changed files
src/pvxs/data.h | 2 +-

## pva-rs mapping
- crates/epics-pva-rs/src/protocol/decode.rs + encode.rs

## Commit message
Fix missing pointer dereference

## Key diff (first 100 lines)
```diff
diff --git a/src/pvxs/data.h b/src/pvxs/data.h
index 114552f..b4a2f4e 100644
--- a/src/pvxs/data.h
+++ b/src/pvxs/data.h
@@ -406,7 +406,7 @@ public:
     TypeDef& operator+=(const Iterable& children) {
         auto edit = _append_start();
         for(auto& child : children) {
-            _append(edit, child);
+            _append(*edit, child);
         }
         _append_finish(std::move(edit));
         return *this;

```
