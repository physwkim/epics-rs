# 2b16c1c087f3 — fix Value move ctor

**Date**: 2020-02-25  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: eliminated  

## Changed files
src/pvxs/data.h | 14 ++++++++++++--

## pva-rs mapping
- crates/epics-pva-rs/src/protocol/decode.rs + encode.rs

## Commit message
fix Value move ctor

## Key diff (first 100 lines)
```diff
diff --git a/src/pvxs/data.h b/src/pvxs/data.h
index f4658fb..84c425f 100644
--- a/src/pvxs/data.h
+++ b/src/pvxs/data.h
@@ -353,9 +353,19 @@ private:
 public:
     // movable and copyable
     Value(const Value&) = default;
-    Value(Value&&) = default;
+    Value(Value&& o) noexcept
+        :desc(o.desc)
+    {
+        store = std::move(o.store);
+        o.desc = nullptr;
+    }
     Value& operator=(const Value&) = default;
-    Value& operator=(Value&&) = default;
+    Value& operator=(Value&& o) noexcept {
+        store = std::move(o.store);
+        desc = o.desc;
+        o.desc = nullptr;
+        return *this;
+    }
     ~Value();
 
     //! allocate new storage, with default values

```
