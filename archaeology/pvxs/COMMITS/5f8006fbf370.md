# 5f8006fbf370 — fix MCastMembership::operator<

**Date**: 2023-05-16  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: uncertain  

## Changed files
src/osiSockExt.h | 4 ++--

## pva-rs mapping
- (no direct mapping found)

## Commit message
fix MCastMembership::operator<

## Key diff (first 100 lines)
```diff
diff --git a/src/osiSockExt.h b/src/osiSockExt.h
index 39ade62..9650094 100644
--- a/src/osiSockExt.h
+++ b/src/osiSockExt.h
@@ -120,9 +120,9 @@ struct MCastMembership {
     bool operator<(const MCastMembership& o) const {
         if(af==o.af) {
             if(af==AF_INET)
-                return memcmp(&req.in, &o.req.in, sizeof(o.req.in));
+                return memcmp(&req.in, &o.req.in, sizeof(o.req.in))<0;
             else
-                return memcmp(&req.in6, &o.req.in6, sizeof(o.req.in6));
+                return memcmp(&req.in6, &o.req.in6, sizeof(o.req.in6))<0;
         }
         return af<o.af;
     }

```
