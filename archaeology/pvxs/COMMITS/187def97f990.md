# 187def97f990 — fix UDPCollector sharing

**Date**: 2020-02-23  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: eliminated  

## Changed files
src/udp_collector.cpp | 4 ++++

## pva-rs mapping
- crates/epics-pva-rs/src/client_native/udp.rs

## Commit message
fix UDPCollector sharing

## Key diff (first 100 lines)
```diff
diff --git a/src/udp_collector.cpp b/src/udp_collector.cpp
index 842eea3..be79d7c 100644
--- a/src/udp_collector.cpp
+++ b/src/udp_collector.cpp
@@ -268,12 +268,16 @@ UDPCollector::UDPCollector(const std::shared_ptr<UDPManager::Pvt>& manager, cons
 
     if(event_add(rx.get(), nullptr))
         throw std::runtime_error("Unable to create collector Rx event");
+
+    manager->collectors[this->bind_addr] = this;
 }
 
 UDPCollector::~UDPCollector()
 {
     manager->loop.assertInLoop();
 
+    manager->collectors.erase(this->bind_addr);
+
     // we should only be destroyed after that last listener has removed itself
     assert(listeners.empty());
     manager->loop.assertInLoop();

```
