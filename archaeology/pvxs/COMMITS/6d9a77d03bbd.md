# 6d9a77d03bbd — SigInt fix disarm

**Date**: 2023-01-26  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: uncertain  

## Changed files
src/util.cpp | 2 +-

## pva-rs mapping
- crates/epics-pva-rs/src/

## Commit message
SigInt fix disarm

## Key diff (first 100 lines)
```diff
diff --git a/src/util.cpp b/src/util.cpp
index 2dbe85d..750de44 100644
--- a/src/util.cpp
+++ b/src/util.cpp
@@ -287,7 +287,7 @@ struct SigInt::Pvt : public epicsThreadRunable {
         thr.exitWait();
 
         evutil_socket_t expect;
-        while((expect=onsig) != wake.s[1] && !onsig.compare_exchange_strong(expect, EVUTIL_INVALID_SOCKET))
+        while((expect=onsig) == wake.s[1] && !onsig.compare_exchange_strong(expect, EVUTIL_INVALID_SOCKET))
         {
             // signal handler in progress...
             epicsThreadSleep(0.1);

```
