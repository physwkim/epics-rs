# 344a96207f93 — Fix pvxsl: serv->getSource needs order argument

**Date**: 2022-02-20  
**Author**: Bruno Martins  
**Severity**: LOW  
**Verdict**: applies  

## Changed files
ioc/iochooks.cpp | 2 +-

## pva-rs mapping
- (no direct mapping found)

## Commit message
Fix pvxsl: serv->getSource needs order argument

## Key diff (first 100 lines)
```diff
diff --git a/ioc/iochooks.cpp b/ioc/iochooks.cpp
index 3071530..2c43a48 100644
--- a/ioc/iochooks.cpp
+++ b/ioc/iochooks.cpp
@@ -32,7 +32,7 @@ void pvxsl(int detail)
     try {
         if(auto serv = instance.load()) {
             for(auto& pair : serv->listSource()) {
-                auto src = serv->getSource(pair.first);
+                auto src = serv->getSource(pair.first, pair.second);
                 if(!src)
                     continue; // race?
 

```
