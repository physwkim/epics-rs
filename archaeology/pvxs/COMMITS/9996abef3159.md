# 9996abef3159 — fix Value::isMarked parents=true

**Date**: 2020-03-19  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: uncertain  

## Changed files
src/data.cpp | 2 +-

## pva-rs mapping
- crates/epics-pva-rs/src/protocol/decode.rs + encode.rs

## Commit message
fix Value::isMarked parents=true

## Key diff (first 100 lines)
```diff
diff --git a/src/data.cpp b/src/data.cpp
index dec710c..d8d7a7d 100644
--- a/src/data.cpp
+++ b/src/data.cpp
@@ -331,8 +331,8 @@ bool Value::isMarked(bool parents, bool children) const
         auto pdesc = desc;
         auto pstore = store.get();
         while(pdesc!=top->desc.get()) {
-            pdesc -= pdesc->parent_index;
             pstore -= pdesc->parent_index;
+            pdesc -= pdesc->parent_index;
 
             if(pstore->valid)
                 return true;

```
