# 056fb2c27b40 — fix iteration of sub-struct

**Date**: 2020-03-07  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: uncertain  

## Changed files
src/data.cpp | 2 +-

## pva-rs mapping
- crates/epics-pva-rs/src/protocol/decode.rs + encode.rs

## Commit message
fix iteration of sub-struct

## Key diff (first 100 lines)
```diff
diff --git a/src/data.cpp b/src/data.cpp
index 975aa63..a7cbeb0 100644
--- a/src/data.cpp
+++ b/src/data.cpp
@@ -746,7 +746,7 @@ void Value::_iter_fl(Value::IterInfo &info, bool first) const
         throw NoField();
 
     if(info.depth) {
-        info.pos = info.nextcheck = store->index() + (first ? 1u : desc->size());
+        info.pos = info.nextcheck = first ? 1u : desc->size();
 
         if(info.marked)
             _iter_advance(info);

```
