# b3778581156b — fix shared_array operator<<

**Date**: 2020-03-23  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: uncertain  

## Changed files
src/pvxs/sharedArray.h | 6 +++---

## pva-rs mapping
- (no direct mapping found)

## Commit message
fix shared_array operator<<

## Key diff (first 100 lines)
```diff
diff --git a/src/pvxs/sharedArray.h b/src/pvxs/sharedArray.h
index 1f73a1b..05e27ce 100644
--- a/src/pvxs/sharedArray.h
+++ b/src/pvxs/sharedArray.h
@@ -397,9 +397,9 @@ public:
     }
 
     detail::Limiter format() const {
-        return Limiter(this->_data.get(),
-                       this->_count,
-                       detail::CaptureCode<typename std::remove_cv<E>::type>::code);
+        return detail::Limiter(this->_data.get(),
+                               this->_count,
+                               detail::CaptureCode<typename std::remove_cv<E>::type>::code);
     }
 };
 

```
