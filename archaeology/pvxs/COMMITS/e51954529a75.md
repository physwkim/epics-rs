# e51954529a75 — ioc: avoid *NULL on exit when partially initialized

**Date**: 2023-06-27  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: uncertain  

## Changed files
ioc/group.cpp | 8 +++++---

## pva-rs mapping
- (no direct mapping found)

## Commit message
ioc: avoid *NULL on exit when partially initialized

## Key diff (first 100 lines)
```diff
diff --git a/ioc/group.cpp b/ioc/group.cpp
index 9321f7e..1c444b2 100644
--- a/ioc/group.cpp
+++ b/ioc/group.cpp
@@ -43,9 +43,11 @@ IOCGroupConfig& IOCGroupConfig::instance()
 
 void IOCGroupConfigCleanup()
 {
-    epicsGuard<epicsMutex> G(configInstance->groupMapMutex);
-    configInstance->groupMap.clear();
-    configInstance->groupConfigFiles.clear();
+    if(configInstance) {
+        epicsGuard<epicsMutex> G(configInstance->groupMapMutex);
+        configInstance->groupMap.clear();
+        configInstance->groupConfigFiles.clear();
+    }
 }
 
 /**

```
