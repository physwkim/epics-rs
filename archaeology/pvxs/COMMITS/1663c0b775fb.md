# 1663c0b775fb — fix server ExecOp::error()

**Date**: 2020-02-25  
**Author**: Michael Davidsaver  
**Severity**: MEDIUM  
**Verdict**: uncertain  

## Changed files
src/serverget.cpp | 5 ++++-

## pva-rs mapping
- crates/epics-pva-rs/src/server_native/tcp.rs (GET handler)

## Commit message
fix server ExecOp::error()

## Key diff (first 100 lines)
```diff
diff --git a/src/serverget.cpp b/src/serverget.cpp
index 4a8ecd3..3fe5144 100644
--- a/src/serverget.cpp
+++ b/src/serverget.cpp
@@ -47,7 +47,10 @@ struct ServerGPR : public ServerOp
              * PUT w/o subcmd&0x40 and !value
              */
 
-            if(cmd==CMD_GET || (cmd==CMD_PUT && (subcmd&0x40))) {
+            if(!msg.empty()) {
+                // noop
+
+            } else if(cmd==CMD_GET || (cmd==CMD_PUT && (subcmd&0x40))) {
                 if(!value || Value::Helper::desc(value)!=this->type.get())
                     throw std::logic_error("GET must reply with exact type previously passed to connect()");
 

```
