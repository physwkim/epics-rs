# a6b3eb58bd42 — add missing check on invalid Union[] selector

**Date**: 2023-11-10  
**Author**: Michael Davidsaver  
**Severity**: MEDIUM  
**Verdict**: uncertain  

## Changed files
src/dataencode.cpp | 3 +++

## pva-rs mapping
- crates/epics-pva-rs/src/protocol/encode.rs

## Commit message
add missing check on invalid Union[] selector

## Key diff (first 100 lines)
```diff
diff --git a/src/dataencode.cpp b/src/dataencode.cpp
index 0a302c2..06a22d9 100644
--- a/src/dataencode.cpp
+++ b/src/dataencode.cpp
@@ -532,6 +532,9 @@ void from_wire_field(Buffer& buf, TypeStore& ctxt,  const FieldDesc* desc, const
 
                 from_wire_full(buf, ctxt, fld);
                 return;
+
+            } else { // invalid selection
+                buf.fault(__FILE__, __LINE__);
             }
         }
             break;

```
