# a4e974def908 — client: fix batch pop() of exception

**Date**: 2023-05-10  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: uncertain  

## Changed files
src/clientmon.cpp | 4 ++--

## pva-rs mapping
- crates/epics-pva-rs/src/client_native/monitor.rs

## Commit message
client: fix batch pop() of exception

## Key diff (first 100 lines)
```diff
diff --git a/src/clientmon.cpp b/src/clientmon.cpp
index 2759cc3..6d6d4ec 100644
--- a/src/clientmon.cpp
+++ b/src/clientmon.cpp
@@ -160,11 +160,11 @@ struct SubscriptionImpl final : public OperationBase, public Subscription
     {
         {
             if(!queue.empty()) {
-                auto ent(std::move(queue.front()));
 
-                if(!canthrow && ent.exc)
+                if(!canthrow && queue.front().exc)
                     return;
 
+                auto ent(std::move(queue.front()));
                 queue.pop_front();
 
                 if(pipeline) {

```
