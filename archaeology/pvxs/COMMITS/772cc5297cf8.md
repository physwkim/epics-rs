# 772cc5297cf8 — server fix spurious Beacon truncated

**Date**: 2020-02-18  
**Author**: Michael Davidsaver  
**Severity**: MEDIUM  
**Verdict**: applies  

## Changed files
src/server.cpp | 5 +++--

## pva-rs mapping
- crates/epics-pva-rs/src/server_native/tcp.rs

## Commit message
server fix spurious Beacon truncated

## Key diff (first 100 lines)
```diff
diff --git a/src/server.cpp b/src/server.cpp
index f7020bd..5dd4912 100644
--- a/src/server.cpp
+++ b/src/server.cpp
@@ -575,8 +575,9 @@ void Server::Pvt::doBeacons(short evt)
             log_printf(serverio, Warn, "Beacon tx error (%d) %s\n",
                        err, evutil_socket_error_to_string(err));
 
-        } else if(unsigned(ntx)<beaconMsg.size()) {
-            log_printf(serverio, Warn, "Beacon truncated %u", unsigned(dest.size()));
+        } else if(unsigned(ntx)<pktlen) {
+            log_printf(serverio, Warn, "Beacon truncated %u < %u",
+                       unsigned(ntx), unsigned(pktlen));
         }
     }
 

```
