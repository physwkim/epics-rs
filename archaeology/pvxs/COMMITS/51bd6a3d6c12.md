# 51bd6a3d6c12 — ioc: fix LocalFieldLog "fast path"

**Date**: 2023-06-12  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: uncertain  

## Changed files
ioc/localfieldlog.cpp | 2 +-

## pva-rs mapping
- (no direct mapping found)

## Commit message
ioc: fix LocalFieldLog "fast path"

## Key diff (first 100 lines)
```diff
diff --git a/ioc/localfieldlog.cpp b/ioc/localfieldlog.cpp
index 7b2280c..fadb85e 100644
--- a/ioc/localfieldlog.cpp
+++ b/ioc/localfieldlog.cpp
@@ -14,7 +14,7 @@ namespace ioc {
 
 LocalFieldLog::LocalFieldLog(dbChannel* pDbChannel, db_field_log* existingFieldLog)
         :pFieldLog(existingFieldLog) {
-    if (pDbChannel && !pFieldLog && (ellCount(&pDbChannel->pre_chain) != 0 || ellCount(&pDbChannel->pre_chain) == 0)) {
+    if (pDbChannel && !pFieldLog && (ellCount(&pDbChannel->pre_chain) != 0 || ellCount(&pDbChannel->post_chain) != 0)) {
         pFieldLog = db_create_read_log(pDbChannel);
         if (pFieldLog) {
             pFieldLog = dbChannelRunPreChain(pDbChannel, pFieldLog);

```
