# 37f5391864e9 — Value::as(T&) return false on transform error

**Date**: 2020-12-24  
**Author**: Michael Davidsaver  
**Severity**: MEDIUM  
**Verdict**: uncertain  

## Changed files
src/pvxs/data.h | 6 +++++-

## pva-rs mapping
- crates/epics-pva-rs/src/protocol/decode.rs + encode.rs

## Commit message
Value::as(T&) return false on transform error

## Key diff (first 100 lines)
```diff
diff --git a/src/pvxs/data.h b/src/pvxs/data.h
index cbceb86..8572385 100644
--- a/src/pvxs/data.h
+++ b/src/pvxs/data.h
@@ -600,7 +600,11 @@ public:
         typename impl::StoreAs<T>::store_t temp;
         auto ret = tryCopyOut(&temp, impl::StoreAs<T>::code);
         if(ret) {
-            val = impl::StoreTransform<T>::out(temp);
+            try {
+                val = impl::StoreTransform<T>::out(temp);
+            }catch(std::exception&){
+                ret = false;
+            }
         }
         return ret;
     }

```
