# 785e180f9bce — evhelper: ensure work dtor before notify

**Date**: 2020-12-29  
**Author**: Michael Davidsaver  
**Severity**: LOW  
**Verdict**: partial  

## Changed files
src/evhelper.cpp | 3 ++-

## pva-rs mapping
- (no direct mapping found)

## Commit message
evhelper: ensure work dtor before notify

## Key diff (first 100 lines)
```diff
diff --git a/src/evhelper.cpp b/src/evhelper.cpp
index fb7c30c..dea6b01 100644
--- a/src/evhelper.cpp
+++ b/src/evhelper.cpp
@@ -202,7 +202,8 @@ struct evbase::Pvt : public epicsThreadRunable
         }
         for(auto& work : todo) {
             try {
-                work.fn();
+                auto fn(std::move(work.fn));
+                fn();
             }catch(std::exception& e){
                 if(work.result) {
                     Guard G(lock);

```
